package com.ria4.odoo.presentation.utils

import com.ria4.odoo.R
import com.ria4.odoo.domain.fetcher.result_listener.RequestType


/** Type aliases for common Android resource references: strings (S), colors (C), drawables (D), layouts (L). / Alias de type pour les références de ressources Android courantes : strings (S), couleurs (C), drawables (D), layouts (L). */

typealias S = R.string
typealias C = R.color
typealias D = R.drawable
typealias L = R.layout